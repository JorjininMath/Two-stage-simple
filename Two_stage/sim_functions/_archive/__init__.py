"""Archived simulator definitions (not registered).

These are the original variance-normalized versions of the nongauss A1/B2/C1
DGPs, kept for reference. The active registry now uses un-normalized versions
where scale = sigma_tar(x) directly (see parent package).
"""
