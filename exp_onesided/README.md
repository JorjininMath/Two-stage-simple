# Legacy one-sided entry point

This directory is compatibility-only. Active code, configuration, and outputs
belong under `experiments/onesided/`.

Use the canonical command for new work:

```bash
python -m experiments.onesided.run_onesided_compare --help
```

The legacy command `python exp_onesided/run_onesided_compare.py` remains
available for existing scripts.
